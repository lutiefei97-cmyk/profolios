# 作品集内容编辑器使用说明

## 打开编辑器

1. 进入 `website` 文件夹。
2. 双击 `start-editor.cmd`。
3. 保持弹出的黑色窗口开启；浏览器会自动进入编辑器。

如果浏览器没有自动打开，在地址栏输入 `http://127.0.0.1:4175/editor/`。

## 基本操作

左侧选择“全局、主页、作品、简历、联系”。作品和简历还会在左侧显示可排序的内容列表。右侧填写内容，中间会自动刷新预览。

- 修改文字：直接输入，离开输入框后可用“撤销”。
- 替换素材：点击“上传替换”，选择 PNG、JPG、GIF、WebP、SVG 或 MP4。
- 调整顺序：使用列表旁的上移、下移按钮。
- 增删内容：使用“＋ 添加”或“×”按钮。删除前会二次确认。
- 检查不同屏幕：预览上方切换电脑、平板、手机。
- 正式发布：确认预览后点击右上角“保存并发布”。

## 调整作品素材

在左侧选择项目和具体作品，再展开右侧“素材显示”。最常用的是：

- 卡片内素材大小：只影响底部缩略卡片。
- 大图内素材大小：只影响选中后的大图。
- 卡片图片区高度：让同组动作保持统一画布高度。
- 水平/垂直位置：素材偏离中心时微调。
- 完整显示：尽量显示全部素材；铺满裁切：会裁掉边缘。
- 像素图清晰放大：像素素材建议开启，普通照片可关闭。

“单张/动图”适合 PNG 和 GIF；“场景叠层”用于多层场景；“多图矩阵”用于一组设施；“图片＋说明”和“流程图”会显示额外的文字列表。

## 备份与恢复

- 每次“保存并发布”前，旧内容都会自动存到 `content/backups/`。
- “导出备份”会下载当前编辑中的 JSON 文件。
- “导入备份”只进入预览，仍需点击“保存并发布”才会覆盖正式内容。

编辑器上传的新素材存放在 `assets/uploads/项目名/`。不要在编辑器运行时移动这些文件。

## 关闭

保存完成后，可以关闭浏览器编辑器页，再回到黑色窗口按 `Ctrl+C`。正式网站的 `index.html` 仍可直接打开，不需要保持编辑器运行。

## 更新线上网站

1. 确认编辑器中已经点击“保存并发布”。
2. 关闭编辑器后，双击网站根目录的 `publish-update.cmd`。
3. 脚本会列出本次变更；确认无误后输入 `Y` 并回车。
4. 看到 `Published successfully` 后等待几分钟，再刷新线上网站。

脚本只允许从 `main` 分支发布，并且不会上传 `content/backups/`、`output/`、`tmp/` 等本地文件。如果显示 GitHub 登录或网络错误，本地修改不会丢失；解决登录或网络问题后重新双击脚本即可。
