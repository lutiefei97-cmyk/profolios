# 桌面作品集视觉规范

## 核心形式

网站模拟一个 Win98 桌面。桌面快捷方式打开主页、作品、简历和联系窗口；作品窗口内部再按不同项目分页。底部任务栏显示已打开的窗口，默认桌面保持纯色，打开蛋岛模拟器后才显示动态场景和鸟类素材。

## 推荐检查尺寸

- 主要展示：1440 × 900 或更大。
- 窄桌面：1100 × 820。
- 手机兜底：390 × 844。

## 作品展示原则

作品页固定为四层信息：项目入口、项目标题、当前作品大图与说明、分类和缩略卡片。桌面端使用单排横向缩略片带，移动端先显示作品列表、点击后进入窗口内详情。每个素材都可以在编辑器中独立调整：

- 卡片内素材大小与大图内素材大小分开控制。
- 默认使用“完整显示”，避免裁掉作品。
- 同类动画尽量使用统一卡片图片区高度。
- 128 × 128 与 64 × 64 素材应按角色在项目中的实际比例缩放，不按原始画布像素直接比较。
- 大图区域保持干净，素材自身不额外套装饰框。
- 缩略卡片只承担定位和选择，不在卡片内重复完整说明；复杂作品优先使用清晰的独立预览图。
- 作品详情地址包含项目与作品识别名，刷新、分享和浏览器前进后退必须恢复同一浏览位置。

## 桌宠交互原则

- 桌宠属于桌面环境层，始终位于快捷方式与应用窗口下方，不遮挡或拦截作品窗口。
- 桌宠关闭或素材尚未加载时保留纯色桌面作为兜底，不显示旧版静态场景或鸟群。
- 任务栏中的桌宠按钮只负责显示与隐藏；桌宠显示时继续在后台桌面活动，应用窗口仍保持正常焦点。

## 信息优先级

每项作品优先说明：它是什么、解决了什么问题、体现了哪种能力。项目职责和成果放在项目入口信息中，不在每张卡片重复。

日常调整请使用 `start-editor.cmd`。只有改变整个桌面系统的交互或视觉语言时，才需要修改代码样式。
